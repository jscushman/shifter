<h3>Shift Reservations</h3>
<p>Welcome to the Shift Reservations tool. To make a new reservation, select a calendar from the <%= link_to "Calendars", calendars_path %> page and either click on the desired starting date or on the "New reservation" button.</p>
<br />
<h4>Today's shifters (<%= Date.today.strftime("%A, %B %-d, %Y") %>)</h4>
<table class="table table-striped">
  <thead>
    <tr>
      <th>Calendar</th>
      <th>Shifters</th>
    </tr>
  </thead>

  <tbody>
    <% Calendar.all.each do |calendar| %>
      <tr>
        <td><%= link_to calendar.title, calendar %></td>
        <td>
          <% todaysappointments = calendar.appointments.on(Date.today) %>
          <% if todaysappointments.length > 0 %>
            <% todaysappointments.each do |appointment| %>
              <%= link_to appointment.person.name, appointment.person %>
              (<%= link_to appointment.person.group.name, appointment.person.group %>) &mdash;
              Day <%= day_of_shift appointment, Date.today %> of <%= total_shift_days appointment %>
              (<%= link_to "Details", appointment %>)<br />
            <% end %>
          <% else %>
            No shifters
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>


<h4>Tomorrow's shifters (<%= Date.tomorrow.strftime("%A, %B %-d, %Y") %>)</h4>
<table class="table table-striped">
  <thead>
    <tr>
      <th>Calendar</th>
      <th>Shifters</th>
    </tr>
  </thead>

  <tbody>
    <% Calendar.all.each do |calendar| %>
      <tr>
        <td><%= link_to calendar.title, calendar %></td>
        <td>
          <% todaysappointments = calendar.appointments.on(Date.tomorrow) %>
          <% if todaysappointments.length > 0 %>
            <% todaysappointments.each do |appointment| %>
              <%= link_to appointment.person.name, appointment.person %>
              (<%= link_to appointment.person.group.name, appointment.person.group %>) &mdash;
              Day <%= day_of_shift appointment, Date.tomorrow %> of <%= total_shift_days appointment %>
              (<%= link_to "Details", appointment %>)<br />
            <% end %>
          <% else %>
            No shifters
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
